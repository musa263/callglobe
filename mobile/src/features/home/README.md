# Mobile Home

`screens/HomeScreen` composes the personal/business home, business line summary
and recent calls. It consumes feature contexts rather than becoming a second
calling engine. Missing numbers must remain unassigned. Validate both modes,
loading/empty data and navigation; run mobile typecheck after prop changes.
