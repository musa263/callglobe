diff --git a/node_modules/@telnyx/react-native-voice-sdk/lib/call.ts b/node_modules/@telnyx/react-native-voice-sdk/lib/call.ts
index 7776025..f9e2b1a 100644
--- a/node_modules/@telnyx/react-native-voice-sdk/lib/call.ts
+++ b/node_modules/@telnyx/react-native-voice-sdk/lib/call.ts
@@ -480,7 +480,7 @@ export class Call extends EventEmitter<CallEvents> {
    * close the peer connection, and set the call state to 'ended'.
    * @param customHeaders Optional custom headers to include with the hangup request
    */
-  public hangup = (customHeaders?: { name: string; value: string }[]) => {
+  public hangup = async (customHeaders?: { name: string; value: string }[]) => {
     // Log local hangup to call report
     this.callReportCollector?.log('info', 'Call ended', {
       cause: 'USER_BUSY',
@@ -490,7 +490,7 @@ export class Call extends EventEmitter<CallEvents> {
     // Stop debug stats collection before ending the call
     this.stopDebugStats();
 
-    this.connection.send(
+    await this.connection.sendAndWait(
       createHangupRequest({
         callId: this.callId,
         telnyxLegId: this.telnyxLegId!,
@@ -505,6 +505,34 @@ export class Call extends EventEmitter<CallEvents> {
     this.closePeerAfterCallReport('done');
     this.setState('ended');
   };
+  /**
+   * Restart ICE and renegotiate the active Telnyx media dialog after a network
+   * interface migration. The attach transaction is Telnyx's supported
+   * re-INVITE equivalent and carries the fresh SDP offer to the gateway.
+   */
+  public restartMedia = async () => {
+    if (!this.peer) throw new Error('[Call] Peer is not created');
+    const peerConnection = this.peer.getPeerConnection();
+    if (!peerConnection) throw new Error('[Call] Peer connection is unavailable');
+    peerConnection.restartIce();
+    await this.peer.createOffer({ iceRestart: true });
+    const response = await this.connection.sendAndWait(
+      createAttachMessage({
+        callId: this.callId,
+        sessionId: this.sessionId,
+        sdp: this.getLocalSdpForSignaling(),
+        customHeaders: this.inviteCustomHeaders || [],
+        destinationNumber: this.options.destinationNumber || '',
+        callerIdName: this.options.callerIdName || '',
+        callerIdNumber: this.options.callerIdNumber || '',
+        clientState: this.options.clientState || '',
+        userVariables: [],
+      })
+    );
+    if (!response || typeof response !== 'object') throw new Error('[Call] Invalid media restart acknowledgment');
+    if (this.isTrickleIceEnabled()) this.peer.flushPendingLocalCandidates();
+  };
+
 
   /**
    * Hold the call
diff --git a/node_modules/@telnyx/react-native-voice-sdk/lib/client-options.ts b/node_modules/@telnyx/react-native-voice-sdk/lib/client-options.ts
index edab35e..d5b21a3 100644
--- a/node_modules/@telnyx/react-native-voice-sdk/lib/client-options.ts
+++ b/node_modules/@telnyx/react-native-voice-sdk/lib/client-options.ts
@@ -28,6 +28,8 @@ export interface ClientOptions {
    * Outbound calls can override this per call with peerConnectionOptions.useTrickleIce.
    * @default false
    */
+  /** ICE servers used by inbound calls and as the default for outbound calls. */
+  iceServers?: RTCIceServer[];
   useTrickleIce?: boolean;
   /**
    * The push notification device token, used for receiving push notifications on a specific device.
diff --git a/node_modules/@telnyx/react-native-voice-sdk/lib/client.ts b/node_modules/@telnyx/react-native-voice-sdk/lib/client.ts
index 74f800d..f26feda 100644
--- a/node_modules/@telnyx/react-native-voice-sdk/lib/client.ts
+++ b/node_modules/@telnyx/react-native-voice-sdk/lib/client.ts
@@ -431,7 +431,13 @@ export class TelnyxRTC extends EventEmitter<TelnyxRTCEvents> {
       telnyxSessionId: null,
       telnyxLegId: null,
       callId: null,
-      options,
+      options: {
+        ...options,
+        peerConnectionOptions: {
+          ...options.peerConnectionOptions,
+          iceServers: options.peerConnectionOptions?.iceServers ?? this.options.iceServers,
+        },
+      },
       debug: this.options.debug,
       callReportConfig: this.getCallReportConfig(),
       pushWhenActive: this.options.pushWhenActive,
@@ -1124,6 +1130,7 @@ export class TelnyxRTC extends EventEmitter<TelnyxRTCEvents> {
         options: {
           destinationNumber: msg.params.caller_id_number,
           peerConnectionOptions: {
+            iceServers: this.options.iceServers,
             useTrickleIce: this.options.useTrickleIce,
           },
         },
@@ -1275,6 +1282,7 @@ export class TelnyxRTC extends EventEmitter<TelnyxRTCEvents> {
       options: {
         destinationNumber: msg.params.caller_id_number,
         peerConnectionOptions: {
+          iceServers: this.options.iceServers,
           useTrickleIce: this.options.useTrickleIce,
         },
       },
diff --git a/node_modules/@telnyx/react-native-voice-sdk/lib/connection.ts b/node_modules/@telnyx/react-native-voice-sdk/lib/connection.ts
index 50b0269..48afeec 100644
--- a/node_modules/@telnyx/react-native-voice-sdk/lib/connection.ts
+++ b/node_modules/@telnyx/react-native-voice-sdk/lib/connection.ts
@@ -172,7 +172,14 @@ export class Connection extends EventEmitter<ConnectionEvents> {
     const deferred = createDeferredPromise<unknown>();
     this.transactions.set(msg.id, deferred);
     this.send(msg);
-    return deferred.promise;
+    let timeout: ReturnType<typeof setTimeout>;
+    const deadline = new Promise<never>((_resolve, reject) => {
+      timeout = setTimeout(() => {
+        this.transactions.delete(msg.id);
+        reject(new Error(`Telnyx signaling transaction timed out: ${msg.id}`));
+      }, 12_000);
+    });
+    return Promise.race([deferred.promise, deadline]).finally(() => clearTimeout(timeout));
   };
 
   public close = () => {
diff --git a/node_modules/@telnyx/react-native-voice-sdk/lib/peer.ts b/node_modules/@telnyx/react-native-voice-sdk/lib/peer.ts
index 547b9e8..6d3ac89 100644
--- a/node_modules/@telnyx/react-native-voice-sdk/lib/peer.ts
+++ b/node_modules/@telnyx/react-native-voice-sdk/lib/peer.ts
@@ -95,7 +95,9 @@ export class Peer {
     if (this.iceGatheringComplete) {
       // Inbound pre-accept flows may never await ICE completion. Attach a handler
       // before rejecting so close() is marked handled while awaited callers still reject.
-      this.iceGatheringComplete.promise.catch(() => {});
+      this.iceGatheringComplete.promise.catch((error) => {
+        log.debug('[Peer] ICE gathering promise closed during teardown', error);
+      });
       this.iceGatheringComplete.reject(new Error('Peer connection closed'));
       this.iceGatheringComplete = null;
     }
@@ -175,13 +177,14 @@ export class Peer {
 
     return this;
   };
-  public createOffer = async () => {
+  public createOffer = async (options?: { iceRestart?: boolean }) => {
     if (!this.instance) {
       throw new Error('[Peer] Cannot create offer before peer connection');
     }
     const offer = await this.instance.createOffer({
       offerToReceiveAudio: true,
       offerToReceiveVideo: false,
+      ...(options?.iceRestart ? { iceRestart: true } : {}),
     });
 
     await this.instance.setLocalDescription(offer).catch((error) => {
