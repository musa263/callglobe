; Generated example only. Production provisioning writes one endpoint, auth,
; and AOR block per tenant extension with a random password.
;
; [2000](vocivo-endpoint)
; auth=2000-auth
; aors=2000
; callerid=User 2000 <2000>
;
; [2000-auth](vocivo-auth)
; username=org_primary_2000
; password=replace-with-random-secret
;
; [2000](vocivo-aor)
