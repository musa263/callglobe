# Included from the sip.vocivo.app vhost `location /ws`.
# iOS URLSessionWebSocketTask sends Sec-WebSocket-Protocol: sip and requires the
# proxy to echo it. Without this header the native SIP phone never REGISTERs.

proxy_http_version 1.1;
proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection "upgrade";
proxy_set_header Host $host;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_set_header Sec-WebSocket-Protocol $http_sec_websocket_protocol;
proxy_read_timeout 3600s;
proxy_send_timeout 3600s;
