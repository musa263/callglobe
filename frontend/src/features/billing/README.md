# Web Wallet

`WalletView` renders balance, rates and permitted top-up interactions using data
provided by the app/API. Administrative ledger management is in admin/WalletsPage;
all balance mutations belong to backend billing. Check loading, empty balance,
failed requests and feature-disabled states; run the web build and UI checks.
