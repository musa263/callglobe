# Product Website

`main.jsx` selects the product subpage and renders its navigation/content;
`landing.css` owns the website styling. HTML entry points remain in `frontend/`
so Vite/Vercel keep the published URLs. This feature does not initialize a phone
or carry authentication/tenant decisions. Test all product URLs and mobile navigation
after changes; run the frontend build to verify every HTML entry.
